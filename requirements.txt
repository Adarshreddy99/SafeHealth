# SafeHealth — Classifier Dependencies
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.0.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.29.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyarrow>=14.0.0       # for reading parquet files

# Utilities
tqdm>=4.65.0
